trigger:
  - main 

pool:
  vmImage: 'windows-latest'

steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.7'  

  - script: |
      python hello.py
    displayName: 'Ejecutar script Python'

